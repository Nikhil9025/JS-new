// while loop .

let index = 0;
while (index <= 10) {
    console.log(`The value of Index is = ${index}`);
    index = index + 2;
}

// while loop on Array .

const myArray = ["SuperMan" , "Batman" , "Splash"];

let arr = 0;
while (arr < myArray.length) {
    console.log(myArray[arr]);
    arr++;  // arr = arr + 1;
}

// do while loop .

let marks = 1;
 do {
    console.log(`My Marks ${marks}`);
    marks++;
 } while (marks <= 10);  // if the condition will false . it runs atleast once .
